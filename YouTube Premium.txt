<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YouTube Premium Hub</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #000000 0%, #1a0000 25%, #330000 50%, #660000 75%, #cc0000 100%);
            min-height: 100vh;
            color: white;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 50px;
            padding: 40px 20px;
        }

        .header h1 {
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 15px;
            background: linear-gradient(45deg, #ff4757, #ff6b7a);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-shadow: 0 0 30px rgba(255, 71, 87, 0.3);
            animation: glow 2s ease-in-out infinite alternate;
        }

        @keyframes glow {
            from { text-shadow: 0 0 20px rgba(255, 71, 87, 0.3); }
            to { text-shadow: 0 0 40px rgba(255, 71, 87, 0.6); }
        }

        .header p {
            font-size: 1.2rem;
            opacity: 0.8;
            margin-bottom: 30px;
        }

        .legend {
            display: flex;
            justify-content: center;
            gap: 30px;
            margin-bottom: 40px;
            flex-wrap: wrap;
        }

        .legend-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 15px 25px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 25px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .legend-box {
            width: 25px;
            height: 25px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
        }

        .legend-available {
            background: linear-gradient(135deg, #2ed573, #1e90ff);
            border: 2px solid rgba(46, 213, 115, 0.5);
            box-shadow: 0 4px 15px rgba(46, 213, 115, 0.3);
        }

        .legend-occupied {
            background: linear-gradient(135deg, #ff4757, #c44569);
            border: 2px solid rgba(255, 71, 87, 0.5);
            box-shadow: 0 4px 15px rgba(255, 71, 87, 0.3);
        }

        .accounts-container {
            display: grid;
            grid-template-columns: repeat(6, 1fr);
            grid-template-rows: repeat(4, 1fr);
            gap: 20px;
            max-width: 800px;
            margin: 0 auto 50px;
            padding: 40px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 20px;
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
        }

        .account {
            width: 100px;
            height: 100px;
            border-radius: 15px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            position: relative;
            overflow: hidden;
            border: 3px solid transparent;
        }

        .account::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: left 0.6s;
        }

        .account:hover::before {
            left: 100%;
        }

        .account-number {
            font-size: 0.9rem;
            margin-top: 5px;
            opacity: 0.8;
        }

        .account.available {
            background: linear-gradient(135deg, #2ed573, #1e90ff);
            border-color: rgba(46, 213, 115, 0.6);
            box-shadow: 0 8px 25px rgba(46, 213, 115, 0.4);
        }

        .account.available:hover {
            transform: translateY(-10px) scale(1.05);
            box-shadow: 0 20px 40px rgba(46, 213, 115, 0.6);
            border-color: rgba(46, 213, 115, 1);
        }

        .account.occupied {
            background: linear-gradient(135deg, #ff4757, #c44569);
            border-color: rgba(255, 71, 87, 0.6);
            cursor: not-allowed;
            opacity: 0.7;
            box-shadow: 0 8px 25px rgba(255, 71, 87, 0.4);
        }

        .account.occupied:hover {
            transform: scale(0.95);
            opacity: 0.5;
        }

        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(5px);
            animation: fadeIn 0.3s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .modal-content {
            background: linear-gradient(135deg, #2c3e50, #34495e);
            margin: 5% auto;
            padding: 40px;
            border-radius: 20px;
            width: 90%;
            max-width: 500px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
            animation: slideUp 0.4s ease;
        }

        @keyframes slideUp {
            from { transform: translateY(50px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        .close {
            color: #aaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
            cursor: pointer;
            transition: color 0.3s;
        }

        .close:hover {
            color: #ff4757;
        }

        .form-group {
            margin-bottom: 25px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #ecf0f1;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 15px;
            border: 2px solid rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            background: rgba(255, 255, 255, 0.05);
            color: white;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: #ff4757;
            box-shadow: 0 0 15px rgba(255, 71, 87, 0.3);
            background: rgba(255, 255, 255, 0.1);
        }

        .form-group textarea {
            resize: vertical;
            min-height: 100px;
        }

        .btn {
            color: white;
            padding: 15px 30px;
            border: none;
            border-radius: 50px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 600;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
            width: 100%;
            margin-top: 10px; /* Butonlar arası boşluk için margin-top kullanıldı */
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.6s;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(0,0,0, 0.3);
        }

        @media (max-width: 768px) {
            .header h1 {
                font-size: 2.5rem;
            }
            
            .accounts-container {
                grid-template-columns: repeat(4, 1fr);
                grid-template-rows: repeat(6, 1fr);
                gap: 15px;
                padding: 20px;
            }
            
            .account {
                width: 80px;
                height: 80px;
                font-size: 1rem;
            }

            .account-number {
                font-size: 0.8rem;
            }
            
            .modal-content {
                margin: 10% auto;
                padding: 30px 20px;
            }
        }

        @media (max-width: 480px) {
            .accounts-container {
                grid-template-columns: repeat(3, 1fr);
                grid-template-rows: repeat(8, 1fr);
            }
            
            .account {
                width: 70px;
                height: 70px;
                font-size: 0.9rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1><img src="data:image/svg+xml;base64," style="width: 48px; height: 48px; margin-right: 15px; vertical-align: middle;"> YouTube Premium</h1>
            <p>Uygun YouTube Premium Hesabını Seç!</p>
        </div>

        <div class="legend">
            <div class="legend-item">
                <div class="legend-box legend-available">✅</div>
                <span>Uygun Hesap</span>
            </div>
            <div class="legend-item">
                <div class="legend-box legend-occupied">❌</div>
                <span>Dolu Hesap</span>
            </div>
        </div>

        <div class="accounts-container" id="accountsContainer">
            </div>
    </div>

    <div id="requestModal" class="modal">
        <div class="modal-content">
            <span class="close">&times;</span>
            <h2 style="margin-bottom: 20px; color: #ff4757;">📋 Premium Hesap Talebi</h2>
            <form id="requestForm" onsubmit="event.preventDefault();">
                <div class="form-group">
                    <label for="name">Ad Soyad</label>
                    <input type="text" id="name" name="name" required>
                </div>
                <div class="form-group">
                    <label for="phone">İletişim (WhatsApp) Numarası</label>
                    <input type="tel" id="phone" name="phone" placeholder="+90 555 123 45 67" required>
                </div>
                <div class="form-group">
                    <label for="request">Talep Detayı</label>
                    <textarea id="request" name="request" placeholder="YouTube Premium hesabı ile ilgili talebinizi detaylarıyla açıklayın..." required></textarea>
                </div>
                <button type="button" class="btn" onclick="sendToWhatsApp()" style="background: linear-gradient(135deg, #25D366, #128C7E); margin-bottom: 10px;">📱 WhatsApp'a Gönder</button>
                <button type="button" class="btn" onclick="sendToTelegram()" style="background: linear-gradient(135deg, #0088cc, #229ED9);">✈️ Telegram'a Gönder</button>
            </form>
        </div>
    </div>

    <script>
        // Hesapları oluştur
        const accountsContainer = document.getElementById('accountsContainer');
        const totalAccounts = 24;
        const occupiedAccounts = [2, 5, 8, 11, 14, 17, 20, 23]; // Dolu hesaplar

        for (let i = 1; i <= totalAccounts; i++) {
            const account = document.createElement('div');
            account.className = occupiedAccounts.includes(i) ? 'account occupied' : 'account available';
            
            const statusDiv = document.createElement('div');
            statusDiv.textContent = occupiedAccounts.includes(i) ? '❌' : '✅';
            statusDiv.style.fontSize = '1.5rem';
            
            const numberDiv = document.createElement('div');
            numberDiv.className = 'account-number';
            numberDiv.textContent = `#${i}`;
            
            account.appendChild(statusDiv);
            account.appendChild(numberDiv);
            account.setAttribute('data-account', i);
            if (!occupiedAccounts.includes(i)) {
                account.addEventListener('click', () => openModal(i));
            }
            
            accountsContainer.appendChild(account);
        }

        // Modal işlemleri
        const modal = document.getElementById('requestModal');
        const closeBtn = document.querySelector('.close');
        const form = document.getElementById('requestForm');
        let selectedAccount = null;
        function openModal(accountNumber) {
            selectedAccount = accountNumber;
            modal.style.display = 'block';
        }

        closeBtn.addEventListener('click', () => {
            modal.style.display = 'none';
        });
        window.addEventListener('click', (e) => {
            if (e.target === modal) {
                modal.style.display = 'none';
            }
        });
        
        // Form alanlarının dolu olup olmadığını kontrol eden ortak fonksiyon
        function validateForm() {
            const name = document.getElementById('name').value;
            const phone = document.getElementById('phone').value;
            const request = document.getElementById('request').value;
            
            if (!name || !phone || !request) {
                alert('Lütfen tüm alanları doldurun!');
                return false;
            }
            return { name, phone, request };
        }

        // WhatsApp gönderme fonksiyonu
        function sendToWhatsApp() {
            const formData = validateForm();
            if (!formData) return;
            
            const message = `🎬 *YouTube Premium - Yeni Talep* 🎉\n\n` +
                          `👤 *Ad Soyad:* ${formData.name}\n` +
                          `📱 *İletişim Numarası:* ${formData.phone}\n` +
                          `💳 *Seçilen Hesap No:* #${selectedAccount}\n\n` +
                          `📝 *Talep Detayı:*\n${formData.request}\n\n` +
                          `📅 *Tarih:* ${new Date().toLocaleString('tr-TR')}`;
            
            const encodedMessage = encodeURIComponent(message);
            const whatsappURL = `https://wa.me/+905373028325?text=${encodedMessage}`;
            
            try {
                window.open(whatsappURL, '_blank');
                alert('📱 WhatsApp\'a yönlendiriliyorsunuz...');
                postSubmission();
            } catch (error) {
                console.error('WhatsApp yönlendirme hatası:', error);
                alert('WhatsApp açılamadı. Lütfen tekrar deneyin.');
            }
        }
        
        // Telegram gönderme fonksiyonu
        function sendToTelegram() {
            const formData = validateForm();
            if (!formData) return;

            const message = `🎬 YouTube Premium - Yeni Talep\n\n` +
                          `👤 Ad Soyad: ${formData.name}\n` +
                          `📱 WhatsApp: ${formData.phone}\n` +
                          `💳 Seçilen Hesap: #${selectedAccount}\n` +
                          `📝 Talep: ${formData.request}\n\n` +
                          `📅 Tarih: ${new Date().toLocaleString('tr-TR')}`;

            const telegramUsername = 'randumann';
            const encodedMessage = encodeURIComponent(message);
            const telegramURL = `https://t.me/${telegramUsername}?text=${encodedMessage}`;
            
            try {
                window.open(telegramURL, '_blank');
                alert('✈️ Telegram\'a yönlendiriliyorsunuz...');
                postSubmission();
            } catch (error) {
                console.error('Telegram yönlendirme hatası:', error);
                alert(`Telegram açılamadı. Lütfen manuel olarak @${telegramUsername} kullanıcısına mesajınızı gönderin.`);
            }
        }
        
        // Gönderim sonrası ortak işlemler
        function postSubmission() {
            markAccountAsOccupied();
            modal.style.display = 'none';
            form.reset();
        }

        // Hesabı dolu olarak işaretleme fonksiyonu
        function markAccountAsOccupied() {
            const accountElement = document.querySelector(`[data-account="${selectedAccount}"]`);
            if(accountElement){
                accountElement.className = 'account occupied';
                const statusElement = accountElement.querySelector('div:first-child');
                statusElement.textContent = '❌';
                accountElement.removeEventListener('click', openModal);
            }
        }
        
        // Sayfa yüklendiğinde animasyon
        window.addEventListener('load', () => {
            const accounts = document.querySelectorAll('.account');
            accounts.forEach((account, index) => {
                account.style.opacity = '0';
                account.style.transform = 'translateY(30px)';
                
                setTimeout(() => {
                    account.style.transition = 'all 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94)';
                    account.style.opacity = '1';
                    account.style.transform = 'translateY(0)';
                }, index * 80);
            });
        });
    </script>
</body>
</html>